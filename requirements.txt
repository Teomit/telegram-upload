# pyrofork is the active Pyrogram fork; both ship and import as `pyrogram`.
# Upstream pyrogram 2.0.x doesn't run on Python 3.13+ (asyncio.get_event_loop
# RuntimeError), pyrofork patches that.
pyrofork>=2.3,<3.0
# AES-IGE acceleration for pyrogram. tgcrypto isn't built for Python 3.13+;
# cryptography is much faster than the pure-Python pyaes fallback.
cryptography>=42.0
click>=8.0.0,<9.0.0
pysocks>=1.7.1,<2.0.0
more-itertools>=8.0.0,<12.0.0
# ffmpeg + ffprobe must be on PATH (not pip packages). Required for
# video thumbnails and metadata. Install via system package manager.
